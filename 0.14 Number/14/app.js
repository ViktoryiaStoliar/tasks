// Ввести строку. Если эта строка равна “hschool”, вывести true. В противном случае false.
// Добавить проверки для строк. Удалить лишние пробелы

const str = prompt(`Введите строку`);

if (isNaN) {
    console.log(str.includes(`hschool`) ? `true` : `false`);
    console.log(str.replace(/ /g, ''));
}